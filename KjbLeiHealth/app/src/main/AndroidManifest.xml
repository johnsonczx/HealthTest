<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.njgroup.health">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.flash" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".heart_rate.HeartRateActivity"
            android:screenOrientation="portrait"
            android:theme="@style/HeartRateTheme" />
        <activity
            android:name=".ear_listen.EarListenActivity"
            android:screenOrientation="portrait"
            android:theme="@style/EarListenTheme" />
        <activity
            android:name=".breath_speed.BreathSpeedActivity"
            android:screenOrientation="portrait"
            android:theme="@style/BreathSpeedTheme" />
    </application>

</manifest>
